// import { getPlants, getUser } from '../utils/service';

describe('Utils testing', () => {
  it('Started testing', () => {
    expect(true).toBe(true);
  });

  // Not working since fetch is not defined
  // describe(`Test get User and check it's values`, () => {
  //   it('should get more than 0 plants back', async () => {
  //     const allPlants = await getPlants();
  //     console.log(allPlants);
  //     // expect(allPlants.length > 0).toBe(true);
  //   });
  // });
});
